# 작업 방식

## 승인 절차
- 파일 생성/수정, git 커밋/푸시/브랜치 작업 등 실제 변경을 가하는 작업 전에는
  반드시 "무엇을 어떻게 할 것인지"를 먼저 설명하고, 명확한 승인("응", "진행해줘" 등)을
  받은 뒤에 실행한다.
- 모호한 답변("굿", "ㅇㅋ" 등)은 승인으로 간주하지 않는다.

## Git 작업 규칙
- git 명령 실행 전 `git status`/`git diff`로 현재 상태를 먼저 확인한다.
- 커밋은 관련 없는 변경사항을 섞지 않고, 논리적으로 연관된 파일끼리 나눠서 커밋한다.
- 브랜치를 새로 만들거나 전환하기 전, 원격 `develop`과 최신 상태인지 확인한다.
- 커밋 메시지는 `[타입] 내용` 형식을 따른다.
  (`feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`)

## 프로젝트 구조
- DB 접속 정보(`resources/`)는 `.gitignore`로 제외되어 있다. 커밋하지 않는다.
- DB 스키마는 `schema/DDL.sql`에서 관리한다.
